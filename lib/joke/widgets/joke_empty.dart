import 'package:flutter/material.dart';

class JokeEmpty extends StatelessWidget {
  const JokeEmpty({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
